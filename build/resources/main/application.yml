spring:
  profiles:
    active: local  # 기본은 로컬
---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petsi
    username: root
    password: rladbgus02

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/petsi
---

spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://mysql:3306/petsi
    username: petsi_user
    password: petsi_pass
    hikari:
      initialization-fail-timeout: -1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    mongodb:
      uri: mongodb://mongodb:27017/petsi
